/** App code: consumes a style module's classes and ports at runtime. */

import { fill, fraction, track } from './progress.style'

export const classes = { track, fill }
export const halfway = fraction.set(0.5)
